# BurnMon v0.3, Claude Code session prompts (Sonnet)

Build roadmap for v0.3 (`2026-09-23_v0.3_spec.md`), one prompt per session, seven sessions,
two a week, release Fri 2026-10-09. Start a fresh session per prompt; never run two prompts
in one conversation. Model: Sonnet for every session. Switch to Opus only for a question
the spec does not answer, after the session has stopped and Wilco has agreed.

Runs in: a Claude Code session on the laptop, cwd `C:\ZND\projects\burnmon`, `main` at the
previous session's commit, started with `claude --model claude-sonnet-5`. Paste one block. Every
prompt carries the same rules: house rules from `C:\ZND\AGENTS.md` (no em dashes, full
`C:\ZND\...` paths in prose), never touch `C:\dev\Work`, read the trail before changing a
parser, an open choice means stop and ask Wilco with two or three candidates and a
recommendation, `node --check` on the template script, `go test ./... -count=1` and
`.\build.ps1` green before stopping, any change a user can see checked in the real window
with `scripts\uicheck.ps1` (v0.2.3 W0) and never on a proxy check alone, one paragraph prepended to `SESSION_LOG.md`, one
`hub_agent_update_<date>_v0.3_<session>.md` brief in `04_assets`, the checklist at the
bottom ticked, commit locally, stop before tagging or pushing and print those commands.

## V3-1, price books and the cost function (C1, C2)

```text
Read C:\ZND\projects\burnmon\02_roadmap\2026-09-23_v0.3_spec.md sections 1 and 2.1 C1, C2, and C:\ZND\projects\burnmon\04_assets\2026-09-22_token_monitor_architecture.md section 4.3. Then read internal\pricing\pricing.go. C1: restructure the price books into dated JSON embedded in the binary, three kinds (API list per vendor and model, plan credits, subscription share), each with date and source URL, overridable from burnmon.json with the existing override behaviour kept. Before writing any number, read the live vendor pages: Anthropic and OpenAI API pricing, the GitHub Copilot AI credits table and per-model multipliers, and any published ChatGPT or Codex credit table. Write what you found with today's date into the books; anything you cannot confirm stays out and is listed as VERIFY in SESSION_LOG.md. Hermes: tokens only. Extend burnmon-cli price-check to print every book with date and source. C2: one Go function that returns cost on every basis a book covers for a set of events, plus the headline basis for that vendor given the config (credits for Copilot, subscription share when configured, else API list labelled upper bound). No UI in this session. Tests: a fixture per vendor, each basis, the headline choice, a vendor with no book. Rules from the header of C:\ZND\projects\burnmon\02_roadmap\2026-09-23_v0.3_session_prompts.md apply. Tick V3-1 there.
```

## V3-2, client map and active time (K1, K2)

```text
Read C:\ZND\projects\burnmon\02_roadmap\2026-09-23_v0.3_spec.md sections 2.2 K1 and K2. K1: owner rules in burnmon.json gain optional client and optional remote (a git remote URL pattern). Match order per session: remote rule first (read the remote from the project's .git\config file, never call git), then path rule, then owner default with client unassigned. Migration 6 adds client on sessions, additive. burnmon-cli reown re-applies both. A v0.2 burnmon.json with owners only must load unchanged: add a test with that exact file. Update burnmon.example.json with one commented client example. K2: active time per session as the sum of gaps between consecutive events, any gap above active_idle_minutes (config, default 10) counts zero; per client as the sum of its sessions. Expose both through the Go side, no UI yet beyond what already renders sessions. Spot-check: pick three of Wilco's sessions from yesterday, compare active time with C:\ZND\10_holding\03_logs\time\ for the same day if an entry exists, write the result in SESSION_LOG.md (plan assumption A8). Tests: remote beats path, path fallback, unassigned, a gap exactly at the cutoff. Rules from the header of C:\ZND\projects\burnmon\02_roadmap\2026-09-23_v0.3_session_prompts.md apply. Tick V3-2 there. Tag after commit: v0.3.0-alpha.1.
```

## V3-3, dev and business switch, client view (C3, K3)

```text
Read C:\ZND\projects\burnmon\02_roadmap\2026-09-23_v0.3_spec.md sections 2.1 C3 and 2.2 K3, and C:\ZND\projects\burnmon\04_assets\2026-09-22_burnmon_now_page_features.md section 4 (the dev and business table). Add one toggle in the header, dev by default, start state from "mode" in burnmon.json. Dev shows what it shows today. Business shows euros on the headline basis from the V3-1 cost function with the basis named next to the figure, credits left where the book knows them, the client, and the forecast in euros. Every number on the Now page flips. History gains a client filter next to owner and, in business mode, a per-client table (tokens, headline cost, active time, sessions). Sessions gains a client column. Client controls stay hidden while no client rule exists. Replace every "tokens only until v0.3" string with the real figure or "tokens only" for vendors without a book. Then section 2.5 U1 and U2: move the Now intro text to About, put the no-running-sessions notice as one small line under the live burn chart, add loading states to every Now visual, never show the saved-report text while the bindings are still being injected, find and fix why the vendor strip showed no rows after startup, and make an empty axis read 0. Check both modes and the startup with scripts\uicheck.ps1, screenshots listed in SESSION_LOG.md. Rules from the header of C:\ZND\projects\burnmon\02_roadmap\2026-09-23_v0.3_session_prompts.md apply. Tick V3-3 there.
```

## V3-3b, monitor mode (U3, U4)

```text
Read C:\ZND\projects\burnmon\02_roadmap\2026-09-23_v0.3_spec.md section 2.5 U3 and U4, and the v0.2.2 colour table in internal\report\template.html. Build monitor mode: one view that fills the window with the live burn chart, the running sessions and the vendor strip, no header and no tab bar, one small "Full view" switch back. The choice is stored as "view" ("monitor" or "full") in burnmon.json through the same save path the settings dialog uses, and the settings dialog gets a default-view field. In business mode monitor mode uses the normal visuals with euros. In dev mode monitor mode is real text: a monospace dark page, the chart drawn with block characters (one column per minute over the 30-minute window, one row set per session, vendor colours), sessions and the vendor strip as text boxes with ASCII borders, no hover or bar click, a session box opens the turn drawer for its last turn. Reuse bmLive and bmVendorStrip as they are, no new backend calls. Check with scripts\uicheck.ps1: open in monitor mode from a saved setting, switch to full and back, restart and confirm the choice is kept, dev and business screenshots, all listed in SESSION_LOG.md. Rules from the header of C:\ZND\projects\burnmon\02_roadmap\2026-09-23_v0.3_session_prompts.md apply. Tick V3-3b there.
```

## V3-4, export and merge (K4, K5)

```text
Read C:\ZND\projects\burnmon\02_roadmap\2026-09-23_v0.3_spec.md sections 2.2 K4 and K5 and the wall rule in C:\ZND\projects\burnmon\02_roadmap\2026-09-22_v0.2_spec.md section 2.2 P6. Build burnmon-cli export with the flags and fields in K4. The label defaults to dev-1 and never reads the hostname. When owner rules exist, --owner is required and the command refuses without it. No paths, session ids, prompts or project names leave in the file; add a test that scans an export for any path separator or session id pattern and fails on one. Build burnmon-cli merge per K5: merged.json plus a static report.html that opens offline with no network script (inline Chart.js from the vendored copy if a chart is needed), one column per label, totals per client, vendor and week, no cap, refuses a different schema value. Run a real export from this laptop with --owner ZND, confirm no Valona row, and merge it with a second export made with a different label; open report.html and describe it in SESSION_LOG.md. Rules from the header of C:\ZND\projects\burnmon\02_roadmap\2026-09-23_v0.3_session_prompts.md apply. Tick V3-4 there. Tag after commit: v0.3.0-alpha.2.
```

## V3-5, Copilot in VS Code and macOS/Linux builds (A4, B1)

Before this session Wilco copies his real Copilot VS Code OTel span file, prompt text
stripped, into `C:\ZND\projects\burnmon\testdata\copilotvsc\`.

```text
Read C:\ZND\projects\burnmon\02_roadmap\2026-09-23_v0.3_spec.md sections 2.3 A4 and 2.4 B1, and the A3 entry in SESSION_LOG.md (2026-09-23). A4 first: internal\adapter\copilotvsc tails the OTel file, 5-second poll, last write wins per request, tokens and model from the gen_ai span attributes, project from a workspace attribute where present. Open the fixture and the live file first and write the real attribute names in SESSION_LOG.md before parsing. Add the two VS Code settings to the README. B1: a GitHub Actions workflow that builds burnmon and burnmon-cli for darwin and linux, amd64 and arm64, CGO_ENABLED=0, and attaches them to a tag release. Off Windows the app opens its page in the default browser; WebView2 stays behind its Windows build tag. Add default roots for Claude, Codex, Hermes and Copilot on macOS and Linux from each tool's docs, marked VERIFY where a doc is silent. README and About say untested on macOS and Linux. If the session runs out of room, B1 slips first, then A4: stop, say which, and do not half-build either. Rules from the header of C:\ZND\projects\burnmon\02_roadmap\2026-09-23_v0.3_session_prompts.md apply. Tick V3-5 there. Tag after commit: v0.3.0-beta.1.
```

## V3-6, release candidate and v0.3.0 (Done when, VERIFY, docs)

```text
Read C:\ZND\projects\burnmon\02_roadmap\2026-09-23_v0.3_spec.md sections 5 and 6. Run every Done-when item against the built exe and the real local store, not fixtures, and write pass or fail with evidence per item in SESSION_LOG.md. Resolve every VERIFY item to a stated source and date, or to an explicit "still unverified" label in the UI where it applies. Fix a fail only if it is small and in scope; otherwise stop and ask. Then: version constants to 0.3.0 in cmd\burnmon\main.go and cmd\burnmon-cli\main.go, README and STATUS.md rewritten for v0.3, DEADLINES.md and 02_roadmap\roadmap.md updated, the Groundwork Kit note in README (how Talon installs, sets mode business, exports). Rules from the header of C:\ZND\projects\burnmon\02_roadmap\2026-09-23_v0.3_session_prompts.md apply. Tick V3-6 there. Tag after commit: v0.3.0.
```

## Checklist

- [x] V3-1 price books and cost function
- [ ] V3-2 client map and active time (`v0.3.0-alpha.1`)
- [ ] V3-3 switch, client view, Now page fixes and loading states
- [ ] V3-3b monitor mode
- [ ] V3-4 export and merge (`v0.3.0-alpha.2`)
- [ ] V3-5 Copilot VS Code and platforms (`v0.3.0-beta.1`)
- [ ] V3-6 release (`v0.3.0`, 2026-10-09)
