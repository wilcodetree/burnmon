# BurnMon v0.2, Claude Code session prompts (Sonnet)

Build roadmap for v0.2 (`2026-09-22_v0.2_spec.md`), one prompt per session, fourteen
sessions in weeks 39 to 46, two a week. Start a fresh session per prompt; never run two
prompts in one conversation. Model: Sonnet for every session (mechanics against a decided
spec). Switch to Opus only for a question the spec does not answer, and then only after
the session has stopped and Wilco has agreed.

Runs in: a Claude Code session on the laptop, cwd `C:\ZND\projects\burnmon`, `main` at the
previous session's commit. Paste one block. Every prompt carries the same rules, so each
session is self-contained: house rules from `C:\ZND\AGENTS.md` (no em dashes, full
`C:\ZND\...` paths in prose), never touch `C:\dev\Work`, read the trail before changing a
parser, an open choice means stop and ask Wilco with two or three candidates and a
recommendation, `go test ./... -count=1` and `.\build.ps1` green before stopping, one
paragraph prepended to `SESSION_LOG.md`, the checklist at the bottom of this file ticked,
stop before tagging or pushing.

Session 39A is the v0.1.2 patch (`2026-09-22_v0.1.2_patch_spec.md`); it runs before
anything in v0.2 and is included here so the whole sequence is one file.

## 39A, v0.1.2 patch

Have a Codex session and a Claude Code session running while this session works.

```text
Read C:\ZND\projects\burnmon\02_roadmap\2026-09-22_v0.1.2_patch_spec.md and work through F7, F8, F9 in that order. F7 first: a new Codex session appeared on the Now page only after Refresh now, so the live watcher missed the new rollout. Confirm which of the three hypotheses in the spec holds by logging every fsnotify event under the Codex root while I start a new Codex session; write the confirmed cause and the measured latency in SESSION_LOG.md; fix it with a test that creates a temp Codex root, a new day folder and a new rollout with one session_meta and one token_count line, and asserts the snapshot shows the session within 2 seconds. F8: rebuild drawNowChart so one Chart.js instance lives for the page and options.animation is false: no bar may ever animate in height, that is the bounce I see now. Every bar is an {x: timestamp, y: tokens} point on a time scale (type 'time'), never a category index; each poll sets data and calls update('none') and moves min to now minus 30 minutes and max to now, so bars keep their height and crawl left 2 seconds per poll, the only motion; the rightmost 10-second slot grows until it closes. Diff sessions into the dataset array by session id in place. Both axis maxima follow smoothedMax = max(peak * 1.1, smoothedMax * 0.95) with a 10K floor; tick labels every 5 minutes on round minutes. Watch it 5 minutes live: no bar changes height except the rightmost. This replaces the F3 rule "Y max recomputed per poll". F9: version constants to 0.1.2 in cmd\burnmon\main.go and cmd\burnmon-cli\main.go, README first section rewritten for BurnMon v0.1.2, STATUS.md filled with what is true, _board rows renamed. go test ./... -count=1 and .\build.ps1 green after each of F7, F8, F9. House rules from C:\ZND\AGENTS.md: no em dashes, full C:\ZND\... paths in prose. Never touch C:\dev\Work. Open choice: stop and ask me with candidates and a recommendation. Write one hub_agent_update_2026-09-2X_burnmon_v0.1.2.md brief in 04_assets (the v0.1.1 pass shipped without one). Prepend one paragraph to SESSION_LOG.md and tick 39A in 02_roadmap\2026-09-22_v0.2_session_prompts.md. Stop before tagging; print the commit and tag commands for v0.1.2.
```

## 39B, migrations and the owner column (S1, P6 store side)

```text
Read C:\ZND\projects\burnmon\02_roadmap\2026-09-22_v0.2_spec.md sections 2.1 S1 and 2.2 P6, then build the store migrations. Add a schema_version table; put one numbered migration per step in internal\store\migrations\ (Go files registered in order, or embedded SQL, your call, state it in SESSION_LOG.md); every migration is additive: add table, add column, backfill, never drop events. Startup runs pending migrations inside one transaction and logs from and to version. Migration 1 records the current v0.1 schema as version 1 without changing it. Migration 2 adds the owner column on sessions (P6) with default empty. Remove the drop-and-rebuild path at internal\store\store.go around line 101. Test: copy a real v0.1 store from this laptop into a temp dir, run to head, assert every v0.1 event is still present with identical ids and the version table reads 2. Also add the owners rule table to config (ordered rules match and owner, default owner personal when rules exist, empty by default) and apply it at ingest to the session's project path; add burnmon-cli reown that re-applies rules to existing sessions. No UI for owners in this session. go test ./... -count=1 and .\build.ps1 green. House rules from C:\ZND\AGENTS.md. Never touch C:\dev\Work. Open choice: stop and ask. Prepend one paragraph to SESSION_LOG.md, tick 39B in this prompts file. Stop before committing; print the commit command.
```

## 40A, tool_calls and CLI live (S2, S3)

```text
Read C:\ZND\projects\burnmon\02_roadmap\2026-09-22_v0.2_spec.md sections 2.1 S2 and S3. Add the tool_calls table through migration 3 (session id, turn, tool name, at, input bytes, result bytes where present). Before writing the extraction, open one real Claude Code transcript and one real Codex rollout on this laptop and confirm where tool calls live (Claude tool_use blocks, Codex function_call items); note the field names in SESSION_LOG.md. Fill tool_calls from both adapters at ingest, backfill existing sessions once behind a meta flag. Add burnmon-cli tools --since 30d --json (tool name, calls, sessions, input and result bytes) and make the Tools tab read the same query. S3: burnmon-cli live must use the windowed EventsSince plus SessionTotals path the app uses, never AllEvents; cmd\burnmon-cli\main.go near line 98. Tests: a fixture per adapter with two tool calls, the tools command output, and a live run on a large store staying under 50 MB. go test ./... -count=1 and .\build.ps1 green. House rules from C:\ZND\AGENTS.md. Never touch C:\dev\Work. Open choice: stop and ask. Prepend one paragraph to SESSION_LOG.md, tick 40A. Stop before committing; print the commit command.
```

## 40B, five tabs, Now default, English only (P1, P4, P5)

```text
Read C:\ZND\projects\burnmon\02_roadmap\2026-09-22_v0.2_spec.md sections 2.2 P1, P4 and P5. In internal\report\template.html: the tab bar becomes Now, History, Sessions, Tools, About, in that order; Now is the start page and the last tab is not remembered; Overview, Months, Weeks, Days and How it works tabs are removed; How it works becomes the first section of About. History in this session is a placeholder tab that says "History arrives in the next session" and nothing else; do not move the old three pages' code yet, leave their render functions in place unexported from the tab bar so 41A can reuse them. Deep links #now, #history, #sessions, #tools, #about. P5: remove the language toggle, the second language in the t() table and the lang setting; keep t() so copy stays in one place; grep the template and the Go side for any remaining Dutch string and list what you removed in SESSION_LOG.md. node --check on the template JS. go test ./... -count=1 and .\build.ps1 green; open the exe and confirm Now opens first. House rules from C:\ZND\AGENTS.md. Never touch C:\dev\Work. Open choice: stop and ask. Prepend one paragraph to SESSION_LOG.md, tick 40B. Stop before tagging; print the commit and tag commands for v0.2.0-alpha.1.
```

## 41A, the History page (P2)

```text
Read C:\ZND\projects\burnmon\02_roadmap\2026-09-22_v0.2_spec.md section 2.2 P2 and look at how the old Months, Weeks and Days render functions in internal\report\template.html build their charts and tables. Build History as one page: filters at the top for period (day, week, month), range (last 7, 30, 90 days, custom from and to), vendor (all plus every agent seen in the store), and owner (only shown when owner rules exist). Below the filters: totals for the filter (tokens by class, sessions, turns; cost only where the price book covers the vendor, otherwise the text "tokens only until v0.3"), then the charts and tables of the old three pages rendered for the chosen period, one visual per question, no duplicate. Filter state lives in the URL hash so a view can be reopened. The data comes from the Go side through one bound function bmHistory(filter) returning the aggregated payload for the filter, never from the page aggregating raw events. Delete the old three tabs' code once History renders everything they did. Test: a store fixture with two vendors over three weeks, bmHistory for week and for month, expected totals. node --check, go test ./... -count=1, .\build.ps1 green. House rules from C:\ZND\AGENTS.md. Never touch C:\dev\Work. Open choice: stop and ask. Prepend one paragraph to SESSION_LOG.md, tick 41A. Stop before committing; print the commit command.
```

## 41B, vendor strip and the Hermes adapter (P3, A1)

Wilco has recorded a short Hermes session and put a copy of its SQLite file in
`C:\ZND\projects\burnmon\testdata\hermes\` with personal content stripped, before this session.

```text
Read C:\ZND\projects\burnmon\02_roadmap\2026-09-22_v0.2_spec.md sections 2.2 P3 and 2.5 A1. P3 first: a vendor strip on the Now page under the session cards, one row per vendor seen in the store, columns today, this week, this month, tokens only, total row last; one SQL query grouped by agent and period, refreshed once a minute from its own timer, not on the 2-second poll; each cell links to History with vendor and period set in the hash. A1: the Hermes adapter under internal\adapter\hermes reading the Hermes SQLite messages table (WAL, open read-only, poll every 5 seconds, no fsnotify). Before writing the parser, open the fixture in C:\ZND\projects\burnmon\testdata\hermes\ and the live Hermes database on this laptop, confirm the table and column names for per-message token counts, model and session, and write them in SESSION_LOG.md; if the context window is not in the data, the card says "context window unknown". Hermes writes nothing to tool_calls in v0.2. Tests: the strip query on a two-vendor fixture; the Hermes fixture parses to the expected turn count and tokens. go test ./... -count=1 and .\build.ps1 green. House rules from C:\ZND\AGENTS.md. Never touch C:\dev\Work. Open choice: stop and ask. Prepend one paragraph to SESSION_LOG.md, tick 41B. Stop before tagging; print the commit and tag commands for v0.2.0-alpha.2.
```

## 42A, insight package, re-prefill and compaction (I1, I2 part 1)

```text
Read C:\ZND\projects\burnmon\02_roadmap\2026-09-22_v0.2_spec.md section 2.3 I1 and I2, and C:\ZND\projects\burnmon\04_assets\2026-09-22_burnmon_now_page_features.md section 3. Create internal\insight: input is the canonical events of one session ordered by turn plus the model's context window from the price book; output is []Finding{Kind, Turn, At, Evidence map[string]float64, Cause, Confidence}; no HTML, no store writes. Implement two rules. re-prefill: cache write in a turn above 20K tokens (threshold from config), cause inferred in this order: compaction just happened, model changed since the previous turn, gap since the previous turn above the cache TTL, first turn after resume, otherwise unknown. compaction: context size drops more than 30 percent between consecutive turns while the session continues. The Claude Code cache TTL and auto-compact threshold are VERIFY: read the current Anthropic pricing and Claude Code docs pages, write what you found with the date in the price-book JSON, and until confirmed the cause text says "gap 8 min, TTL unverified". Expose findings through bmLive for running sessions (computed per poll on the windowed events, measure the cost and keep it under 5 ms per session) and through burnmon-cli insight <session-id> --json. Tests: fixture sessions that trigger each rule and one that triggers none. go test ./... -count=1 and .\build.ps1 green. House rules from C:\ZND\AGENTS.md. Never touch C:\dev\Work. Open choice: stop and ask. Prepend one paragraph to SESSION_LOG.md, tick 42A. Stop before committing; print the commit command.
```

## 42B, context runway and expensive turn (I2 part 2)

```text
Read C:\ZND\projects\burnmon\02_roadmap\2026-09-22_v0.2_spec.md section 2.3 I2 and the internal\insight package as it is now. Add two rules. context-runway: a linear fit over the last 10 turns of context size, reporting turns to 80 and 90 percent of the window; shown on the session card as "about N turns to 80%", and "runway unknown" when the window is unknown or the fit slope is zero or negative. expensive-turn: a turn above the 95th percentile of the session's turn tokens or cost, with the dominant class named in the evidence. Put the runway line on the card and keep everything else for 43A. Tests: a fixture growing 8K per turn against a 200K window gives the expected turn count; a flat session gives no runway finding; a fixture with one 10x turn gives exactly one expensive-turn. Measure bmLive cost with four rules on a real store and write the number in SESSION_LOG.md. go test ./... -count=1 and .\build.ps1 green. House rules from C:\ZND\AGENTS.md. Never touch C:\dev\Work. Open choice: stop and ask. Prepend one paragraph to SESSION_LOG.md, tick 42B. Stop before tagging; print the commit and tag commands for v0.2.0-alpha.3.
```

## 43A, markers, ticker and the spike drawer (I3 on Now)

Have a Claude Code and a Codex session running while this session works.

```text
Read C:\ZND\projects\burnmon\02_roadmap\2026-09-22_v0.2_spec.md section 2.3 I3 and the findings that bmLive now returns. On the Now page: one marker per finding on the live chart at its slot, shaped by kind, without breaking the in-place update from v0.1.2 F8; a turn ticker under the chart, newest first, one line per turn ("14:02:11 Codex CLI, turn 31, 2.1K new, 118K cached") with the finding appended when there is one, capped at 50 lines; the "explain this spike" drawer opening on click of a marker or a ticker line: model, token classes, tool calls that turn (from tool_calls), files read where the trail has them, gap since the previous turn, the finding's cause and confidence, and the session id. The drawer is one template branch fed by one bound function bmTurn(sessionID, turn). Check live for 5 minutes that markers appear within 2 seconds of a turn and the chart still drifts without jumping. node --check, go test ./... -count=1, .\build.ps1 green. House rules from C:\ZND\AGENTS.md. Never touch C:\dev\Work. Open choice: stop and ask. Prepend one paragraph to SESSION_LOG.md, tick 43A. Stop before committing; print the commit command.
```

## 43B, Copilot CLI adapter and the OTel check (A2, A3)

Wilco has recorded a short GitHub Copilot CLI session on his own install before this
session and put a copy of the relevant files in `C:\ZND\projects\burnmon\testdata\copilot\`.

```text
Read C:\ZND\projects\burnmon\02_roadmap\2026-09-22_v0.2_spec.md sections 2.5 A2 and A3, and the Copilot rows in C:\ZND\projects\burnmon\04_assets\2026-09-22_token_monitor_sources_and_facts.md. A2: before writing anything, inspect the live GitHub Copilot CLI data on this laptop and the fixture in C:\ZND\projects\burnmon\testdata\copilot\, confirm or kill assumption A3 from the plan (token totals in data.db, written at session end), write the layout with field names in SESSION_LOG.md. Build internal\adapter\copilotcli accordingly: session totals ingested when the session closes, the Now card for an open Copilot CLI session says "totals at session end", History counts the session once it closes. If the data does not exist on this install, stop, report exactly what the folders contain, and ask me. A3, capped at one hour by the clock: can GitHub Copilot in VS Code emit OpenTelemetry to a local file on this laptop? Try the documented settings, write yes or no with the evidence (settings used, file produced or not) in SESSION_LOG.md, change no product code for it. go test ./... -count=1 and .\build.ps1 green. House rules from C:\ZND\AGENTS.md. Never touch C:\dev\Work. Open choice: stop and ask. Prepend one paragraph to SESSION_LOG.md, tick 43B. Stop before tagging; print the commit and tag commands for v0.2.0-alpha.4.
```

## 44A, findings on the Sessions tab (I3 Sessions side)

```text
Read C:\ZND\projects\burnmon\02_roadmap\2026-09-22_v0.2_spec.md section 2.3 I3, Sessions tab part. For finished sessions on the Sessions tab: a findings column with the count by kind, and an expandable row listing each finding (turn, kind, cause, confidence, evidence numbers) with a click that opens the same drawer as on the Now page through bmTurn. Findings for finished sessions are computed on request through one bound function bmSessionInsight(sessionID), not stored, and cached in page memory for the tab's lifetime. Add an owner column and an owner filter on the Sessions tab when owner rules exist. Measure bmSessionInsight on the longest session in the store and write the time in SESSION_LOG.md; above 200 ms, stop and propose caching for v0.3 rather than building it. node --check, go test ./... -count=1, .\build.ps1 green. House rules from C:\ZND\AGENTS.md. Never touch C:\dev\Work. Open choice: stop and ask. Prepend one paragraph to SESSION_LOG.md, tick 44A. Stop before committing; print the commit command.
```

## 44B, forecast chart behind the gate, scoring week 1 (F1)

```text
Read C:\ZND\projects\burnmon\02_roadmap\2026-09-22_v0.2_spec.md section 2.4 and the forecast rows in C:\ZND\projects\burnmon\02_roadmap\2026-09-22_burnmon_plan.md. Build the forecast on the Now page in tokens: the plan line (last four weeks, weekday aware) and the live line (current rate continued) to end of day and end of month, an error band from the scored weeks. Scoring: at the end of each ISO week, store the forecast made on Monday for that week next to the actual, in a forecast_scores table through migration 4; a week counts as scored when both exist. The gate is visible: until one scored week exists the chart shows the last 7 days of history only and the text "forecast unlocks after the first scored week (week 46)". Start scoring this week. Euros are v0.3; the chart unit is tokens. Tests: a fixture with four weeks gives the expected plan line; zero scored weeks shows the gate; one scored week shows the band. go test ./... -count=1 and .\build.ps1 green. House rules from C:\ZND\AGENTS.md. Never touch C:\dev\Work. Open choice: stop and ask. Prepend one paragraph to SESSION_LOG.md, tick 44B. Stop before tagging; print the commit and tag commands for v0.2.0-beta.1.
```

## 45A, README, STATUS, docs pass

```text
Read C:\ZND\projects\burnmon\02_roadmap\2026-09-22_v0.2_spec.md whole, then C:\ZND\projects\burnmon\README.md, STATUS.md, claudecost.example.json and docs\. Rewrite README.md for BurnMon v0.2: what it reads (Claude Code, Cowork, Codex, Hermes, Copilot CLI), the five tabs, the Now page and its findings, the owner rules, the CLI commands (report, live, tools, insight, reown), config with burnmon.json and the still-supported claudecost.json fallback, what it never does (no server, no network call, no telemetry), and the v0.3 items named as not yet there. Rename claudecost.example.json to burnmon.example.json with the owners table shown empty. Fill STATUS.md with what is true at this commit. Update _board. Remove every remaining "claudecost" in user-facing strings except the fallback file name and the history note. Check every path in the docs exists. No code changes beyond strings and the example file. go test ./... -count=1 and .\build.ps1 green. House rules from C:\ZND\AGENTS.md. Never touch C:\dev\Work. Open choice: stop and ask. Prepend one paragraph to SESSION_LOG.md, tick 45A. Stop before committing; print the commit command.
```

## 45B, buffer, scoring week 2

```text
Read C:\ZND\projects\burnmon\02_roadmap\2026-09-22_v0.2_session_prompts.md checklist and C:\ZND\projects\burnmon\SESSION_LOG.md top entries. This is a buffer session: finish anything unticked from 39B to 45A, in that order, following the original prompt for that session. If everything is ticked, run the "Done when" list in section 5 of C:\ZND\projects\burnmon\02_roadmap\2026-09-22_v0.2_spec.md against the exe with a Claude Code and a Codex session running, write the result per item in SESSION_LOG.md, and fix what fails. Confirm forecast scoring wrote week 45. go test ./... -count=1 and .\build.ps1 green. House rules from C:\ZND\AGENTS.md. Never touch C:\dev\Work. Open choice: stop and ask. Prepend one paragraph to SESSION_LOG.md, tick 45B. Stop before committing; print the commit command.
```

## 46A, release candidate

```text
Read C:\ZND\projects\burnmon\02_roadmap\2026-09-22_v0.2_spec.md section 5 (Done when) and section 6 (VERIFY carried). Run every Done-when item against the exe with a Claude Code and a Codex session running and write pass or fail with evidence per item in SESSION_LOG.md. For each VERIFY item, state what the code assumes today and where that is labelled in the UI. Check a v0.1 store copy opens under this build with no event lost (count before and after). Check no Dutch string, no em dash, no "claudecost" in user-facing strings. Fix what fails, nothing else. Write the hub_agent_update_2026-11-1X_burnmon_v0.2_rc.md brief in 04_assets with the pass and fail list. go test ./... -count=1 and .\build.ps1 green. House rules from C:\ZND\AGENTS.md. Never touch C:\dev\Work. Open choice: stop and ask. Prepend one paragraph to SESSION_LOG.md, tick 46A. Stop before tagging; print the commit and tag commands for v0.2.0-rc.1.
```

## 46B, tag v0.2.0

```text
Read C:\ZND\projects\burnmon\SESSION_LOG.md top entry (the rc.1 results) and C:\ZND\projects\burnmon\02_roadmap\2026-09-22_v0.2_spec.md section 5. Fix the fails listed for rc.1 if Wilco confirmed them, nothing else. Set version constants to 0.2.0 in both binaries. Update STATUS.md, DEADLINES.md (v0.2 done with date), roadmap.md (item 3 done, item 4 next) in C:\ZND\projects\burnmon. Write hub_agent_update_2026-11-14_burnmon_v0.2.md in 04_assets: what shipped, what moved to v0.3, the VERIFY items still open, the forecast scoring state. go test ./... -count=1 and .\build.ps1 green. House rules from C:\ZND\AGENTS.md. Never touch C:\dev\Work. Open choice: stop and ask. Prepend one paragraph to SESSION_LOG.md, tick 46B. Stop before tagging; print the commit and tag commands for v0.2.0.
```

## After each session (PowerShell on the laptop, Wilco)

```powershell
# runs in: PowerShell on the laptop. Replace the message and tag with what the session printed; drop the tag lines when the session printed none.
cd C:\ZND\projects\burnmon
git add -A
git commit -m "v0.2.0-alpha.1: five tabs, Now default, English only"
git tag v0.2.0-alpha.1
git push origin main --tags
```

## Checklist (the session ticks its own line, with the commit hash)

- [x] 39A v0.1.2 patch (F7, F8, F9), tag v0.1.2 (`e99ecf9`)
- [x] 39B migrations, owner column, owners config, reown
- [x] 40A tool_calls, tools --json, CLI live windowed
- [x] 40B five tabs, Now default, English only, tag v0.2.0-alpha.1 (`3d6bc88`)
- [x] 41A History page with filters (`9b509e7`)
- [x] 41B vendor strip, Hermes adapter, tag v0.2.0-alpha.2
- [x] 42A insight package, re-prefill, compaction
- [x] 42B context runway, expensive turn, tag v0.2.0-alpha.3
- [ ] 43A markers, ticker, spike drawer
- [ ] 43B Copilot CLI adapter, OTel check, tag v0.2.0-alpha.4
- [ ] 44A findings on Sessions tab
- [ ] 44B forecast behind the gate, scoring week 1, tag v0.2.0-beta.1
- [ ] 45A README, STATUS, docs pass
- [ ] 45B buffer, Done-when run, scoring week 2
- [ ] 46A release candidate, tag v0.2.0-rc.1
- [ ] 46B tag v0.2.0, hub brief

## Wilco's own steps, before the session that needs them

- Before 41B: record one short Hermes session, copy its SQLite file to `C:\ZND\projects\burnmon\testdata\hermes\`, strip personal content.
- Before 43B: record one short GitHub Copilot CLI session on your own install, copy the relevant files to `C:\ZND\projects\burnmon\testdata\copilot\`.
- Before 39A, 43A, 45B, 46A: have a Claude Code and a Codex session running.
