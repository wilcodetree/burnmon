# BurnMon v0.1, Claude Code session prompt (Sonnet)

One prompt per step. Start a fresh session per step; do not run two steps in one
conversation. Model: Sonnet for all three steps (mechanics against a decided spec); switch
to Opus only for a question the spec does not answer, and then only after the session has
stopped and Wilco has agreed.

Runs in: a Claude Code session on the laptop, cwd `C:\ZND\projects\burnmon`, `main` at the
previous step's tag. Paste one block.

## Step 1 prompt (schema and store)

```text
Read C:\ZND\projects\burnmon\02_roadmap\2026-09-22_v0.1_spec.md and do Step 1 only: the internal\schema Event type, the internal\store SQLite store on modernc.org/sqlite with the events, cursors and meta tables and the "largest output wins" upsert, the internal\adapter interface, and the move of the Claude and Cowork parser from internal\scan\parse.go behind internal\adapter\claude. internal\agg and internal\dataset must build the same Payload from the store. Done when burnmon-cli.exe report from the store equals the v0.0.1 report for the same two months (diff the JSON payload, zero differences apart from meta), go test ./... passes with a fixture trail per adapter under testdata\, and .\build.ps1 is green. Read C:\ZND\AGENTS.md first for house rules: no em dashes, full C:\ZND\... paths in prose. Do not scan C:\dev\Work or any Valona folder. If the spec leaves a choice open, stop and ask me with two or three candidates and your recommendation. Stop before tagging or pushing; print the tag command for v0.1.0-alpha.1 and add one paragraph to SESSION_LOG.md.
```

## Step 2 prompt (Codex adapter)

```text
Read C:\ZND\projects\burnmon\02_roadmap\2026-09-22_v0.1_spec.md and do Step 2 only: the Codex adapter under internal\adapter\codex reading %USERPROFILE%\.codex\sessions (and CODEX_HOME, and WSL distros through the existing discovery) from the cursor offset, never whole-file. Before writing the parser, open one real rollout file on this laptop and confirm the field names for token_count, last_token_usage, total_token_usage, turn_context model, session_meta cwd and originator, and rate_limits; where the spec says "or the names found", use what the file says and note it in SESSION_LOG.md. Add OpenAI price-book entries with the list prices from developers.openai.com/codex/pricing, dated inside the JSON, and the price-check CLI command. Done when my Codex sessions appear in the store with model, turns, tokens and cost, a fixture under testdata\codex\ with three turns and one rate_limits object passes, go test ./... and .\build.ps1 are green. House rules from C:\ZND\AGENTS.md. Never touch C:\dev\Work. Open choice: stop and ask with candidates and a recommendation. Stop before tagging; print the tag command for v0.1.0-alpha.2.
```

## Step 3 prompt (Now page)

```text
Read C:\ZND\projects\burnmon\02_roadmap\2026-09-22_v0.1_spec.md Step 3 and C:\ZND\projects\burnmon\04_assets\2026-09-22_burnmon_now_page_features.md sections 2 and 3, then build the Now page minimum: internal\watch on fsnotify over the adapter roots (WSL roots polled every 5 s; confirm on this laptop whether the watcher sees \\wsl$ and write the answer in SESSION_LOG.md), a bound Go function bmLive() the page polls every 2 s returning the JSON snapshot described in the spec, burnmon-cli.exe live --json printing the same snapshot once, a context_window table in the price-book JSON filled from the vendors' model pages and dated, and a new first tab "Now" in internal\report\template.html: session cards with a context gauge (60, 80, 90 percent bands), the 30-minute live chart on the vendored Chart.js, and the forecast placeholder card showing the last 7 days. No server, no port, no network call. Cache clock and turn ticker only if everything above is done and tested. Done when, with Claude Code and Codex running, both sessions show a context percentage and the chart moves within 2 seconds of a turn, the CLI prints the same sessions, a test appends to a temp fixture trail and asserts the snapshot changes, and .\build.ps1 is green. House rules from C:\ZND\AGENTS.md. Open choice: stop and ask. Stop before tagging; print the tag command for v0.1.0.
```

## After each step (PowerShell on the laptop, Wilco)

```powershell
# runs in: PowerShell on the laptop. Replace the tag with the one the session printed.
cd C:\ZND\projects\burnmon
git add -A
git commit -m "v0.1.0-alpha.1: schema, SQLite store, Claude adapter behind the adapter interface"
git tag v0.1.0-alpha.1
git push origin main --tags
```
