# BurnMon, v0.2 leftovers session prompt (2026-09-23)

Model: Sonnet 5. Mechanical cleanup, no new features, no version bump, no tag.

You are in `C:\ZND\projects\burnmon`. Read `AGENTS.md`, `STATUS.md` and the top entry of
`SESSION_LOG.md` first. House rules from `C:\ZND\AGENTS.md` apply: no em dashes anywhere,
English only in the UI, date-prefixed filenames.

Fix these three leftovers from v0.2. Nothing else.

## 1. "Overview tab" copy in About

`internal\report\template.html` lines 349 and 420 still say "the Overview tab". That tab
was removed in 40B. Check whether the History tab shows the same month figures (the
personal cut of the company Claude bill).

- If it does: change both sentences to say "the History tab". Keep the rest of the wording.
- If it does not: delete both sentences. Do not invent a new place for the figure.

Say in your report which case applied and why. Do not touch the dead `#overview` markup or
any other About text. Run `node --check` on the extracted inline script, then
`go test ./... -count=1` and `.\build.ps1`. All three must be green.

## 2. Uncommitted v0.1.2 patch spec edit

`02_roadmap\2026-09-22_v0.1.2_patch_spec.md` has been modified in the working tree since
before 39B. Show the diff. It should be the F8 sharpening (bars visibly growing from zero on
every render). If the diff is only that, commit it as its own commit. If it contains
anything else, stop and show Wilco the diff before committing.

## 3. Untracked briefs in `04_assets`

List untracked files under `04_assets\` (for example
`hub_agent_update_2026-09-23_45b_done_when_pass.md`). Commit the `hub_agent_update_*.md`
briefs and this prompt file. Leave any other untracked file alone and name it in the report.

## Close

- Update `STATUS.md`: remove the "Overview tab" gap from "Known gaps".
- Prepend one paragraph to `SESSION_LOG.md` (most recent first).
- Write one brief with the `hub-agent-update` skill in `04_assets\`.
- Commit locally. Suggested messages: `docs: About copy no longer names the removed
  Overview tab`, `docs: commit F8 sharpening in v0.1.2 patch spec`, `chore: track v0.2
  hub briefs`. Do not push. Print the push command for Wilco:

```powershell
# runs in: PowerShell on the laptop
cd C:\ZND\projects\burnmon
git push origin main
```

Out of scope: the hub roadmap in `C:\ZND\10_holding` (the hub updates that itself), any
v0.3 work, `C:\dev` in any form.
